# Correção v1.0.1

Esta correção remove a validação excessivamente rígida da chave do Supabase.

## Publicação

Substitua somente:

- `admin.html`
- `js/admin.js`

Depois aguarde o GitHub Pages e pressione `Ctrl + F5`.

A versão de cache passa a ser `v=1010`.

O aviso `favicon.ico 404` também foi eliminado e não tinha relação com o login.
